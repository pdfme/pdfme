| Issue | Verdict | Japanese-friendly summary | Primary files | Rough size | Main risk | Confidence |
| --- | --- | --- | --- | --- | --- | --- |
| #1397 | CLEAR | JP: Undo/redo履歴をページ単位ではなくドキュメント全体（schemasList + pageCursor）で持ち、復元時にテンプレート変更通知と対象ページ移動を行う。 / EN: Store undo/redo entries as document-wide state (`schemasList` + `pageCursor`) and restore them with template change notification and page navigation. | `packages/ui/src/components/Designer/index.tsx`; `packages/ui/src/hooks.ts`; focused Designer/hooks tests | M | Undo restore may need to choose consistent UX for page navigation and must avoid stale canvas/template state after add/remove/external template updates. | 4 |
| #1433 | CLEAR | JP: SVG PDFレンダーで`options.font`をpdf-libの`PDFFont`マップに埋め込み、`page.drawSvg(..., { fonts })`へ渡す。 / EN: In SVG PDF rendering, embed configured `options.font` entries into a pdf-lib `PDFFont` map and pass it to `page.drawSvg(..., { fonts })`. | `packages/schemas/src/graphics/svg.ts`; likely shared/extracted font-embedding helper from `packages/schemas/src/text/pdfRender.ts`; SVG/generator font test | S/M | Reusing text font embedding without duplicating cache/fetch/subset behavior, and matching SVG `font-family` names to configured font keys. | 4 |
| #460 | UNCLEAR | JP: QR/バーコードは現在RGB PNG画像として埋め込まれるため、CMYK化には画像変換・CMYK画像埋め込み・ベクター描画化のどれを採るか設計判断が必要。 / EN: QR/barcodes are currently embedded as RGB PNG images, so CMYK support needs a design choice between image conversion, CMYK image embedding, or vector rendering. | `packages/schemas/src/barcodes/pdfRender.ts`; `packages/schemas/src/barcodes/helper.ts`; `packages/pdf-lib/src/core/embedders/PngEmbedder.ts`; possibly `packages/pdf-lib/src/api/colors.ts`/SVG drawing if vectorized | L | Multiple competing implementations have tradeoffs in pdf-lib scope, output fidelity/file size, browser/node parity, and whether `colorType: cmyk` should affect all image-like schema output or only generated barcodes. | 3 |
